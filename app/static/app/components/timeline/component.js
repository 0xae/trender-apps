requirejs(["trender/app", 'vue'], function (app, Vue) {
    Vue.directive('tx-post-stream', {
        bind: function (el, b, vnode) {
            /*
            var data = b.value;
            var id = '#'+vnode.elm.id;
            
            if (b.value.onUpdate) {                
            } else {
            }
            */
        }
    });
});

